<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eironeia.parking" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:textStyle="bold"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InOut"/>
        <activity android:name=".ListCoches"/>
        <activity android:name=".ListCochesSortida"/>
        <activity android:name=".ListRecaptacio"/>
        <activity android:name=".ListRecaptacioDates"/>
        <activity android:name=".Recaptacio"/>
        <activity android:name=".Recaptacio_Dates"/>
        <activity android:name=".Recaptacio_eleccio"/>
        <activity android:name=".Helper"/>

    </application>

</manifest>
